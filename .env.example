# Compose 讀這份的正本 `.env`（不進 Git）。Local 與 deployment host 都要建立。
# Snapshot root 由 scripts/update-snapshots.sh 建立與維護，不要指向開發用的 checkout。
SNAPSHOT_ROOT=${HOME}/work-agent-snapshots
# Container 使用者的 uid/gid 必須等於執行 docker 的 host 使用者，bind mount 才可寫。
HOST_UID=1000
HOST_GID=1000
