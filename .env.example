# Docker Compose 在 host 上展開 compose.yaml 時使用。
# 這些值不會自動傳進 container；${HOME} 會展開成目前使用者的 home。
SNAPSHOT_ROOT=${HOME}/code
STATE_ROOT=${HOME}/.local/share/work-agent/state
DRAFT_ROOT=${HOME}/.local/share/work-agent/drafts
